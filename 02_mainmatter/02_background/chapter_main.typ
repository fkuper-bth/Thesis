#import "/etc/utils.typ"

= Hintergrund <hintergrund>

Im folgenden Abschnitt wird zunächst das _#utils.gls-short("kite2") Projekt_ näher erläutert (siehe @ausgangslage), welches als Inspiration für diese Arbeit dient und deshalb auch der Ausgangspunkt ist, von dem sich die _Problemstellung_ ableitet, welche in @problemstellung erörtert wird.

Basierend auf den identifizierten Problemen wird in @zielsetzung die _Zielsetzung_ formuliert und in @planung folgt eine kurze Diskussion der _Planung_ der durchzuführenden Arbeiten.

#include "01_ausgangslage.typ"
#pagebreak()

#include "02_problemstellung.typ"
#pagebreak()

#include "03_zielsetzung.typ"
#pagebreak()

#include "04_planung.typ"
