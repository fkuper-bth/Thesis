#import "/etc/utils.typ"

= Einleitung <einleitung>

Für Organisationen aller Art, die eine Zielgruppe über bestimmte Themen auf eine interaktive und spielerische Art und Weise schulen möchten, soll im Rahmen dieses Projektes eine #utils.gls("library") entworfen und umgesetzt werden, um eine #utils.gls("cross_platform") Anwendung zu schaffen, die Nutzer*innen mit Hilfe von #utils.glspl("visual_novel") informiert und gezieltes Feedback zum Spielverlauf über die Verwendung von Large Language Models geben soll.

Orientiert wird sich hierbei an einem laufenden Projekt, anhand dessen eine beispielhafte Umsetzung einer solchen Anwendung erfolgen soll. Zu diesem Zwecke soll eine #utils.gls-short("library") entstehen, welche es auf einfache Art und Weise ermöglicht, interaktive Geschichten, die zuvor mit dem Tool zum Erstellen von interaktiven Geschichten _Twine_ @interactive_fiction_technology_foundation_twine_2025
erstellt wurden, auf verschiedenen Plattformen zu verwenden.

Im Rahmen dieser Arbeit wird also einerseits eine #utils.gls-short("library") konzipiert und umgesetzt, die die Verwendung von Twine Geschichten in Anwendungen auf verschiedenen Plattformen ermöglicht, und andererseits eine solche Anwendung beispielhaft erstellt, die diese #utils.gls-short("library") verwendet, um die Anwendung der #utils.gls-short("library") zu demonstrieren. Diese Anwendung soll sich an der #utils.gls("kite2") @hochschule_heilbronn_kite_2023 Anwendung orientieren, die sich zum Zeitpunkt der Verfassung dieser Arbeit noch in Entwicklung befindet.

#pagebreak()

#include "ausgangslage.typ"
#pagebreak()

#include "problemstellung.typ"
#pagebreak()

#include "zielsetzung.typ"
#pagebreak()

#include "planung.typ"
#pagebreak()

#include "projektinhalt.typ"
#pagebreak()
